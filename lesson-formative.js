(function(){
  'use strict';

  const ITEMS=20;
  const VERSION='20260822-20q-v8';
  const LABELS=['A','B','C','D'];
  const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const norm=v=>String(v??'').trim().toLowerCase().replace(/\s+/g,' ');
  function client(){try{if(window.sb?.from)return window.sb;if(typeof sb!=='undefined'&&sb?.from)return sb;}catch(e){}return null;}
  function lessonId(article){const button=article?.querySelector('button[onclick*="toggleLessonComplete"]');const raw=button?.getAttribute('onclick')||'';const match=raw.match(/toggleLessonComplete\(['\"]([^'\"]+)['\"]/);return match?.[1]||article?.dataset.lessonId||null;}
  function isLesson(article){return !!(article&&lessonId(article)&&article.querySelector('.lesson-content'));}
  function parseOptions(value){if(typeof value==='string'){try{value=JSON.parse(value)}catch(e){value=[]}}if(Array.isArray(value))return value.map(String).filter(Boolean).slice(0,4);if(value&&typeof value==='object')return LABELS.map(k=>value[k]).filter(v=>v!==undefined&&v!==null).map(String).slice(0,4);return [];}
  function prepareQuestion(q,index){const opts=parseOptions(q.options);if(opts.length!==4)return null;const answer=norm(q.correct_answer);let correctIndex=opts.findIndex(x=>norm(x)===answer);if(correctIndex<0&&/^[A-D]$/i.test(String(q.correct_answer||'')))correctIndex=LABELS.indexOf(String(q.correct_answer).toUpperCase());if(correctIndex<0)return null;const correctText=opts[correctIndex],wrong=opts.filter((_,i)=>i!==correctIndex);for(let i=wrong.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[wrong[i],wrong[j]]=[wrong[j],wrong[i]];}const target=index%4,out=[];let wi=0;for(let i=0;i<4;i++)out.push(i===target?correctText:wrong[wi++]);return{q,options:out};}
  async function questionsForLesson(id){const key='lesson-formative:'+id;window.__lessonFormativeQuestionsCache ||= {};if(window.__lessonFormativeQuestionsCache[key])return window.__lessonFormativeQuestionsCache[key];const db=client();if(!db)return [];const {data,error}=await db.from('quiz_questions').select('id,lesson_id,question,options,correct_answer,points,position').eq('lesson_id',id).order('position',{ascending:true});if(error)return [];const rows=(data||[]).slice(0,ITEMS);window.__lessonFormativeQuestionsCache[key]=rows;return rows;}
  function removeOldModuleQuiz(){document.querySelectorAll('#moduleQuiz').forEach(el=>el.remove());document.querySelectorAll('#quizResult').forEach(el=>el.remove());document.querySelectorAll('h3').forEach(h=>{const t=(h.textContent||'').trim().toLowerCase();if(t==='📝 formative quiz'||t==='formative quiz')h.remove();});document.querySelectorAll('button').forEach(btn=>{const t=(btn.textContent||'').trim().toLowerCase();if(t==='submit quiz'&&!btn.closest('.lesson-formative-card'))btn.remove();});document.querySelectorAll('.screen .notice').forEach(n=>{const t=(n.textContent||'').toLowerCase();if(t.includes('complete the formative quiz'))n.innerHTML='<b>Learning activity:</b> Complete the formative assessment attached to each lesson before moving to the next lesson.';});}
  async function build(article){if(!isLesson(article)||article.querySelector('.lesson-formative-card'))return;const id=lessonId(article);const rows=await questionsForLesson(id);if(rows.length!==ITEMS)return;const prepared=rows.map(prepareQuestion).filter(Boolean);if(prepared.length!==ITEMS)return;const answers=Array(ITEMS).fill(null);let current=0;const card=document.createElement('section');card.className='lesson-formative-card';card.dataset.lessonId=id;card.dataset.version=VERSION;card.innerHTML='<div class="lf-head"><div class="lf-kicker">FORMATIVE ASSESSMENT</div><h3>Formative Assessment — 20 Questions</h3><p>This assessment belongs to <b>this lesson only</b>. Answer Question 1, select <b>Next</b>, then continue through Question 20.</p><div class="lf-progress"><span class="lf-progress-bar"></span></div><div class="lf-progress-text"></div></div>';const form=document.createElement('div');form.className='lf-form';const nav=document.createElement('div');nav.className='lf-nav';const previous=document.createElement('button');previous.type='button';previous.className='btn alt';previous.textContent='← Previous';const next=document.createElement('button');next.type='button';next.className='btn';next.textContent='Next →';nav.append(previous,next);const submit=document.createElement('button');submit.type='button';submit.className='btn lf-submit';submit.textContent='SUBMIT FORMATIVE ASSESSMENT';const result=document.createElement('div');result.className='lf-result';result.setAttribute('aria-live','polite');prepared.forEach((item,i)=>{const box=document.createElement('div');box.className='lf-question';const title=document.createElement('h4');title.innerHTML='<span class="lf-number">'+(i+1)+'</span><span>'+esc(item.q.question)+'</span>';box.appendChild(title);const choices=document.createElement('div');choices.className='lf-choices';item.options.forEach((text,j)=>{const button=document.createElement('button');button.type='button';button.className='lf-choice';button.innerHTML='<b>'+LABELS[j]+'.</b> '+esc(text);button.addEventListener('click',()=>{answers[i]=text;choices.querySelectorAll('.lf-choice').forEach(x=>x.classList.remove('selected'));button.classList.add('selected');result.textContent='';result.className='lf-result';update();});choices.appendChild(button);});box.appendChild(choices);form.appendChild(box);});card.append(form,nav,submit,result);function answered(){return answers.filter(Boolean).length;}function update(){card.querySelector('.lf-progress-bar').style.width=(answered()/ITEMS*100)+'%';card.querySelector('.lf-progress-text').textContent='Question '+(current+1)+' of '+ITEMS+' • '+answered()+' answered';previous.disabled=current===0;next.style.display=current===ITEMS-1?'none':'block';submit.style.display=current===ITEMS-1?'block':'none';}function show(index){current=Math.max(0,Math.min(ITEMS-1,index));form.querySelectorAll('.lf-question').forEach((q,i)=>q.classList.toggle('active',i===current));result.textContent='';result.className='lf-result';update();}previous.addEventListener('click',()=>show(current-1));next.addEventListener('click',()=>{if(!answers[current]){result.className='lf-result warning';result.textContent='Please select an answer before going to the next question.';return;}show(current+1);});submit.addEventListener('click',async()=>{const missing=answers.map((v,i)=>v?null:i+1).filter(Boolean);if(missing.length){result.className='lf-result warning';result.textContent='Please answer all 20 questions. Unanswered: '+missing.join(', ');show(missing[0]-1);return;}const db=client();if(!db?.rpc){result.className='lf-result bad';result.textContent='The learning service is not ready. Please refresh and try again.';return;}submit.disabled=true;previous.disabled=true;next.disabled=true;submit.textContent='MARKING…';result.className='lf-result';result.textContent='Marking your formative assessment and saving your result…';try{const payload={};prepared.forEach((item,i)=>payload[item.q.id]=String(answers[i]||''));const {data,error}=await db.rpc('submit_lesson_formative',{p_lesson_id:id,p_answers:payload});if(error)throw error;const row=Array.isArray(data)?data[0]:data;const score=Number(row?.score??0),max=Number(row?.max_score??ITEMS),pct=max?Math.round(score/max*100):0;card.dataset.completed='true';card.dataset.score=String(score);result.className='lf-result '+(pct>=50?'good':'bad');result.innerHTML='<strong>Formative assessment completed.</strong><br>Score: '+score+'/'+max+' ('+pct+'%).<br>'+(pct>=50?'Good work. This lesson assessment has been saved.':'Review this lesson and try the assessment again.');const nextArticle=article.nextElementSibling;if(nextArticle&&isLesson(nextArticle)){const go=document.createElement('button');go.type='button';go.className='btn alt lf-next-lesson';go.textContent='Continue to Next Lesson →';go.addEventListener('click',()=>nextArticle.scrollIntoView({behavior:'smooth',block:'start'}));result.appendChild(go);}}catch(error){result.className='lf-result bad';result.textContent=error?.message||'Unable to mark this assessment. Please try again.';}finally{submit.disabled=false;previous.disabled=false;next.disabled=false;submit.textContent='SUBMIT AGAIN';}});const completeButton=article.querySelector('button[onclick*="toggleLessonComplete"]');if(completeButton)completeButton.before(card);else article.appendChild(card);show(0);}
  function style(){if(document.getElementById('lesson-formative-styles'))return;const s=document.createElement('style');s.id='lesson-formative-styles';s.textContent='.lesson-formative-card{margin:24px 0;padding:14px;border:2px solid #b9d8c7;border-radius:18px;background:#fbfefd;scroll-margin-top:90px}.lf-head{padding:17px;background:#eef7f1;border-radius:14px;margin-bottom:14px;color:#17312a}.lf-kicker{font-size:12px;font-weight:900;color:#075c3a;letter-spacing:.7px}.lf-head h3{margin:5px 0;font-size:21px}.lf-head p{margin:0;color:#65746e;font-size:13px;line-height:1.6}.lf-progress{height:9px;background:#dfeae4;border-radius:99px;overflow:hidden;margin-top:14px}.lf-progress-bar{display:block;height:100%;width:0;background:#075c3a;transition:.2s}.lf-progress-text{font-size:12px;font-weight:800;color:#075c3a;margin-top:7px}.lf-question{display:none;padding:16px;background:#fff;border:1px solid #dfe7e2;border-radius:14px}.lf-question.active{display:block}.lf-question h4{display:flex;gap:10px;align-items:flex-start;margin:0 0 14px;font-size:17px;line-height:1.5}.lf-number{min-width:32px;height:32px;border-radius:10px;background:#e8f3ed;color:#075c3a;display:grid;place-items:center;font-size:13px;font-weight:900}.lf-choices{display:grid;gap:9px;margin-top:12px}.lf-choice{display:block;width:100%;text-align:left;padding:13px;border:2px solid #dfe7e2;border-radius:12px;background:#fff;color:#17312a;min-height:50px;cursor:pointer;font-size:14px}.lf-choice:hover{border-color:#9bc9ad}.lf-choice.selected{border-color:#075c3a;background:#eef7f1}.lf-nav{display:flex;gap:9px;margin-top:12px}.lf-nav button{flex:1}.lf-submit{width:100%;margin-top:10px}.lf-result{margin-top:12px;padding:13px;border-radius:11px;line-height:1.55}.lf-result.good{background:#e9f8ee;color:#075c3a}.lf-result.bad{background:#fff0f1;color:#a3223c}.lf-result.warning{background:#fff8e8;color:#805400}.lf-next-lesson{margin-top:10px}@media(max-width:600px){.lesson-formative-card{padding:10px}.lf-head{padding:14px}.lf-question{padding:14px}.lf-question h4{font-size:16px}.lf-choice{font-size:14px;padding:12px}}';document.head.appendChild(s);}
  function removeOldModuleQuiz(){document.querySelectorAll('#moduleQuiz').forEach(el=>el.remove());document.querySelectorAll('#quizResult').forEach(el=>el.remove());document.querySelectorAll('h3').forEach(h=>{const t=(h.textContent||'').trim().toLowerCase();if(t==='📝 formative quiz'||t==='formative quiz')h.remove();});document.querySelectorAll('button').forEach(btn=>{const t=(btn.textContent||'').trim().toLowerCase();if(t==='submit quiz'&&!btn.closest('.lesson-formative-card'))btn.remove();});document.querySelectorAll('.screen .notice').forEach(n=>{const t=(n.textContent||'').toLowerCase();if(t.includes('complete the formative quiz'))n.innerHTML='<b>Learning activity:</b> Complete the formative assessment attached to each lesson before moving to the next lesson.';});}
  let running=false;async function decorate(){if(running)return;running=true;try{removeOldModuleQuiz();const articles=[...document.querySelectorAll('article')].filter(isLesson);for(const article of articles){try{await build(article)}catch(e){console.warn('Lesson formative build failed',e);}}removeOldModuleQuiz();}finally{running=false;}}
  function boot(){style();decorate();let n=0;const timer=setInterval(()=>{decorate();if(++n>80)clearInterval(timer)},500);new MutationObserver(()=>decorate()).observe(document.body,{childList:true,subtree:true});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
